% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SNPData-aggregation.R
\name{aggregate_count_df}
\alias{aggregate_count_df}
\alias{aggregate_count_df,SNPData-method}
\title{Get aggregated SNP count summary by any barcode_info column}
\usage{
aggregate_count_df(x, group_by, test_maf = TRUE)

\S4method{aggregate_count_df}{SNPData}(x, group_by, test_maf = TRUE)
}
\arguments{
\item{x}{A SNPData object}

\item{group_by}{Character vector specifying the column name(s) in barcode_info to group by. When
more than one column is given, counts are aggregated over each unique combination of values
across those columns, and each grouping column is retained in the output.}

\item{test_maf}{Logical (default \code{TRUE}). Whether to include a test_maf column.}
}
\value{
A tibble with columns: snp_id, gene_name, chrom, pos, strand (if available in snp_info), [group_by], ref_count, alt_count, total_count, ref_ratio, maf, (optionally test_maf)
}
\description{
Returns a long-format data frame of reference and alternate allele counts per SNP and aggregated by the specified grouping column from barcode_info.
}
\examples{
\dontrun{
snp_data <- get_example_snpdata()
# Aggregate by donor
aggregate_count_df(snp_data, "donor")

# Aggregate by clonotype
aggregate_count_df(snp_data, "clonotype")

# Aggregate by donor and cell_type combined
aggregate_count_df(snp_data, c("donor", "cell_type"))
}
}
\seealso{
Other count aggregation functions:
\code{\link[=barcode_count_df]{barcode_count_df()}},
\code{\link[=clonotype_count_df]{clonotype_count_df()}},
\code{\link[=donor_count_df]{donor_count_df()}}
}
\concept{count aggregation functions}
