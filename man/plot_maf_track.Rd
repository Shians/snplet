% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_maf_track}
\alias{plot_maf_track}
\title{Plot minor allele frequency track}
\usage{
plot_maf_track(allele_count_df, facet, x_range)
}
\arguments{
\item{allele_count_df}{A data.frame, required, with columns \code{pos}
(position), \code{maf} (minor allele frequency), and a column to facet by
(see \code{facet}). SNP information.}

\item{facet}{Bare column name (unquoted), required. Variable in
\code{allele_count_df} to facet the plot by.}

\item{x_range}{Numeric vector of length 2, required. Genomic range limits to display.}
}
\value{
A ggplot object showing MAF values across the genomic region
}
\description{
This function creates a visualization of minor allele frequencies (MAF) across
a genomic region, with optional faceting by a grouping variable.
}
\examples{
\dontrun{
snp_data <- get_example_snpdata()
allele_df <- donor_count_df(snp_data)
plot_maf_track(allele_df, facet = donor, x_range = c(1000000, 2000000))
}
}
\seealso{
Other genomic track plotting functions:
\code{\link[=plot_gene_anno_track]{plot_gene_anno_track()}},
\code{\link[=plot_maf_pval_track]{plot_maf_pval_track()}}
}
\concept{genomic track plotting functions}
