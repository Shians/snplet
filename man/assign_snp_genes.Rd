% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_snp_genes.R
\name{assign_snp_genes}
\alias{assign_snp_genes}
\title{Assign overlapping gene names to SNPs}
\usage{
assign_snp_genes(snp_df, gene_anno)
}
\arguments{
\item{snp_df}{Data frame with columns \code{chrom} and \code{pos}.}

\item{gene_anno}{Data frame with columns \code{chrom}, \code{start},
\code{end}, and \code{gene_name}.}
}
\value{
A data frame matching \code{snp_df} with an added \code{gene_name} column.
}
\description{
Takes SNP coordinates and gene annotations, then adds a gene_name column
to each SNP with all overlapping genes collapsed by ", ".
}
\examples{
snp_df <- data.frame(chrom = c("chr1", "chr1"), pos = c(100, 500))
gene_anno <- data.frame(
    chrom = c("chr1", "chr1"),
    start = c(50, 450),
    end = c(150, 550),
    gene_name = c("GENE1", "GENE2")
)
assign_snp_genes(snp_df, gene_anno)
}
