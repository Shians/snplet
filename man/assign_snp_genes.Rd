% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/molecule_phasing.R
\name{assign_snp_genes}
\alias{assign_snp_genes}
\title{Assign each SNP to at most one overlapping gene}
\usage{
assign_snp_genes(snp_info, gene_anno)
}
\arguments{
\item{snp_info}{A data.frame/tibble with columns `snp_id`, `chrom`, `pos`.}

\item{gene_anno}{A data.frame/tibble with columns `chrom`, `start`, `end`,
`gene_name`, one row per gene body.}
}
\value{
A tibble with columns `snp_id` and `gene_name`, containing only
  the SNPs that overlap exactly one gene.
}
\description{
Deliberately distinct from `add_snp_gene_names()`, which comma-joins every
overlapping gene into a single label -- a fine display value, but the wrong
grain for molecule counting: a SNP overlapping two gene bodies cannot have
its molecules attributed to either one without guessing, so it is dropped
from the gene-count join here rather than double-counted or mislabelled.
Such a SNP can still take part in `phase_snps()`, since phasing does not
depend on gene assignment.
}
\seealso{
Other molecule-level allele counting functions:
\code{\link[=add_molecule_phase]{add_molecule_phase()}},
\code{\link[=extract_snp_calls]{extract_snp_calls()}},
\code{\link[=molecule_snp_alleles]{molecule_snp_alleles()}},
\code{\link[=phase_snps]{phase_snps()}}
}
\concept{molecule-level allele counting functions}
