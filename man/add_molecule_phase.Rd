% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/molecule_phasing.R
\name{add_molecule_phase}
\alias{add_molecule_phase}
\title{Add read-backed molecule phase to a SNPData object's donor SNP metadata}
\usage{
add_molecule_phase(
  x,
  bam_files,
  target_chrom = "chrX",
  min_mapq = 20L,
  min_baseq = 10L,
  min_molecules = 5L,
  min_consistency = 0.9,
  threads = 4L
)
}
\arguments{
\item{x}{A SNPData object that has already been fit by `assign_xci()` or
`assign_xci_by_clonotype()` (see `.has_xci_diagnostics()`), with a
`donor` column in `barcode_info`.}

\item{bam_files}{A named character vector or list, `donor = path`, one
indexed BAM per donor. Names must match `get_barcode_info(x)$donor`. Any
entry named `"doublet"` or `"unassigned"` is dropped with a warning --
neither is a real donor with its own genotype to phase against.}

\item{target_chrom}{Canonical chromosome to restrict het-SNP selection to.
Default `"chrX"`, matching `assign_xci()`'s own restriction.}

\item{min_mapq, min_baseq, threads}{Passed to `extract_snp_calls()`.}

\item{min_molecules, min_consistency}{Passed to `phase_snps()`.}
}
\value{
A SNPData object with `donor_snp_info` gaining the columns
  `allele_on_x1_em` (the EM's own phase, unchanged, given its own name for
  symmetry with `allele_on_x1_molecule`), `allele_on_x1_molecule`,
  `phase_block`, `phase_source`, `phase_conflict`, and a re-derived
  `allele_on_x1`: the EM's value where present (so nothing downstream that
  already reads `allele_on_x1` changes behaviour), else the molecule value,
  else `NA` where `phase_conflict` is `TRUE`. Also carries a
  `"molecule_calls"` attribute (a tibble with columns `donor`, `barcode`,
  `umi`, `snp_id`, `allele`, the per-donor `molecule_snp_alleles()` output
  already computed here) so `haplotype_expression_by_molecule()` does not
  need to re-extract from the BAM.
}
\description{
Extracts molecule-level allele calls from one BAM per donor, phases them
with `phase_snps()`, orients the resulting blocks to X1/X2 against
`assign_xci()`'s already-stored EM phase (see `.orient_phase_blocks()`),
and writes the result into `donor_snp_info`. Additive only:
`assign_xci()`'s own phase calls are never replaced, and
`haplotype_expression()` is unaffected unless it is told to read the new
columns.
}
\seealso{
Other molecule-level allele counting functions:
\code{\link[=assign_snp_genes]{assign_snp_genes()}},
\code{\link[=extract_snp_calls]{extract_snp_calls()}},
\code{\link[=molecule_snp_alleles]{molecule_snp_alleles()}},
\code{\link[=phase_snps]{phase_snps()}}

Other X-chromosome inactivation functions:
\code{\link[=assign_xci]{assign_xci()}},
\code{\link[=assign_xci_by_clonotype]{assign_xci_by_clonotype()}},
\code{\link[=haplotype_expression]{haplotype_expression()}},
\code{\link[=haplotype_expression_by_molecule]{haplotype_expression_by_molecule()}},
\code{\link[=plot_xci_heatmap]{plot_xci_heatmap()}},
\code{\link[=xci_assignments]{xci_assignments()}},
\code{\link[=xci_haplotypes]{xci_haplotypes()}}
}
\concept{X-chromosome inactivation functions}
\concept{molecule-level allele counting functions}
