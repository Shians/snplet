% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/molecule_phasing.R
\name{.map_bases_to_reads}
\alias{.map_bases_to_reads}
\title{Map each SNP position into query coordinates for every overlapping read}
\usage{
.map_bases_to_reads(galn, snp_gr, snp_ids)
}
\arguments{
\item{galn}{A GAlignments object.}

\item{snp_gr}{A GRanges of target SNPs, width 1, same seqlevels as `galn`.}

\item{snp_ids}{Character vector of SNP identifiers, same length and order
as `snp_gr`.}
}
\value{
A tibble with columns `snp_idx` (position in `snp_gr`/`snp_ids`),
  `read_idx` (position in `galn`), `snp_id`, `base` (the aligned base), and
  `base_quality` (Phred-scaled integer).
}
\description{
Inlines the logic of `GenomicAlignments:::.pileLettersOnSingleRefAt` so the
read of origin survives, precisely what is needed to group calls into
molecules.
}
\keyword{internal}
