% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/molecule_phasing.R
\name{molecule_read_strand}
\alias{molecule_read_strand}
\title{Resolve the alignment strand of each molecule}
\usage{
molecule_read_strand(reads)
}
\arguments{
\item{reads}{A tibble, required, as returned by `extract_snp_calls()$reads`,
with columns `barcode`, `umi`, `qname`, `strand`.}
}
\value{
A tibble with one row per (`barcode`, `umi`) and columns
  `barcode`, `umi`, `strand` (the majority call, `"+"` or `"-"`).
}
\description{
A molecule is one transcript, so every read behind it should agree on
alignment strand; a majority vote absorbs the rare mismapped or chimeric
read rather than letting one read decide. This is alignment strand only;
converting it to the strand of the original transcript (needed to
disambiguate a SNP overlapping genes on opposite strands) additionally
requires the BAM's sense/antisense orientation, since some demultiplexing
pipelines flip reads relative to the transcript (see
`.infer_bam_strand_orientation()`).
}
\seealso{
Other molecule-level allele counting functions:
\code{\link[=add_molecule_phase]{add_molecule_phase()}},
\code{\link[=assign_snp_genes]{assign_snp_genes()}},
\code{\link[=extract_snp_calls]{extract_snp_calls()}},
\code{\link[=molecule_snp_alleles]{molecule_snp_alleles()}},
\code{\link[=phase_snps]{phase_snps()}}
}
\concept{molecule-level allele counting functions}
